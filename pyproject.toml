[project]
name = "scrapper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.42.25",
    "duckdb>=1.4.3",
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
